version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Say Hello
          command: echo "Hello, CircleCI!"
          
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
